# HPSv2 (Human Preference Score v2) verifier
# Scores images based on human preferences for text-image alignment.
# Requires: pip install imscore einops

verifier:
  name: hpsv2
  model_name: "RE-N-Y/hpsv21"
  batch_size: 32


