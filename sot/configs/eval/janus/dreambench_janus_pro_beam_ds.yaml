# @package _global_

# DreamBench Beam Search with Janus Pro + DreamSim Verifier

defaults:
  - /eval/base
  - /components/ar_priors/janus/janus_pro
  - /components/search_algorithms/janus/beam_janus
  - /components/verifiers/dreamsim

dataset:
  name: dreambench
  dataset_dir: datasets/dreambench
