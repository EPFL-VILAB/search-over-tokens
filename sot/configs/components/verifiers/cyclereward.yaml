# CycleReward verifier for text-image alignment
# Uses a cycle-consistent reward model.
# Requires: pip install imscore einops

verifier:
  name: cyclereward
  model_name: "NagaSaiAbhinay/CycleReward-Combo"
  batch_size: 32


